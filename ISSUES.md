# ISSUES / WĄTPLIWOŚCI

## 2026-05-19

- Nie mogę przejrzeć kodu w katalogach `SpaceshipGame`, `bmad-module-game-dev-studio` i `github-and-vscode-vibecoding-config`, bo aktualnie są one pustymi checkoutami submodułów (w repo są tylko wskaźniki `gitlink`).
- Do dalszego przeglądu potrzebne jest pobranie zawartości tych submodułów (lub dostarczenie kodu bezpośrednio w tym repo).

### Pomysł na usprawnienie

- Dodać krótki `README.md` z instrukcją inicjalizacji submodułów, np. `git submodule update --init --recursive`, żeby kolejne przeglądy mogły ruszyć od razu.
